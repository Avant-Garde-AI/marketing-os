{
  "name": "@avant-garde/skill-social-media",
  "version": "0.1.0",
  "description": "Social Media Agent skill pack (spec 24, SM0): social/ artifact formats (strategy, calendar, post) + ungated planning read tools. No publishing, no Actions — those land in SM2 on the spec 20 framework.",
  "type": "module",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "instructions.md"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@avant-garde/skill-kit": "workspace:*",
    "yaml": "^2.4.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.2",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Avant-Garde-AI/marketing-os",
    "directory": "packages/skills/social-media"
  }
}
